import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.fx64b.clock',
  appName: 'Clock',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
